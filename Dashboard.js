import React from 'react';

const Dashboard = ({ sessions }) => {
  // Renders a list of past sessions and stats
  return (
    <div>
      <h2>Dashboard</h2>
      {/* Mapping through sessions to display them */}
    </div>
  );
};

export default Dashboard;
